[defaults]
roles_path = ./roles
inventory = ./inventories/hosts.yaml
# YAML-formatted task output via the built-in default callback
# (the community.general.yaml callback was removed in community.general 12.0.0)
callback_result_format = yaml
